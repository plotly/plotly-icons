{
  "name": "plotly-icons",
  "version": "1.0.1",
  "description": "set of plotly icons",
  "main": "lib/index.js",
  "scripts": {
    "build": "yarn run clean && mkdir build && node scripts/generate.js && babel build --out-dir lib",
    "clean": "rimraf build && rimraf publish/*.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "mkdir lib & babel src/index.js --out-dir lib",
    "buildPub": "yarn build && yarn prepublishOnly"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/plotly/plotly-icons.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/plotly/plotly-icons/issues"
  },
  "homepage": "https://github.com/plotly/plotly-icons#readme",
  "dependencies": {
    "fs": "0.0.1-security",
    "mdi-react": "^2.1.19"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-plugin-transform-export-extensions": "^6.8.0",
    "babel-plugin-transform-object-rest-spread": "^6.8.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1",
    "rimraf": "^2.6.1"
  }
}
